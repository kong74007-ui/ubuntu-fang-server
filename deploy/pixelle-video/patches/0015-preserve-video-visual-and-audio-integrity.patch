diff --git a/pixelle_video/services/frame_processor.py b/pixelle_video/services/frame_processor.py
index fdc6dd2..3db519a 100644
--- a/pixelle_video/services/frame_processor.py
+++ b/pixelle_video/services/frame_processor.py
@@ -848,2 +848,3 @@ class FrameProcessor:
         from pixelle_video.services.frame_html import HTMLFrameGenerator
+        from pixelle_video.services.video_overlay import resolve_video_overlay_template
-        from pixelle_video.utils.template_util import resolve_template_path
+        from pixelle_video.utils.template_util import get_template_type, resolve_template_path
@@ -852 +853,6 @@ class FrameProcessor:
-        template_path = resolve_template_path(config.frame_template)
+        template_path = Path(resolve_template_path(config.frame_template))
+        if (
+            frame.media_type == "video"
+            and get_template_type(config.frame_template or "") != "video"
+        ):
+            template_path = resolve_video_overlay_template(template_path)
@@ -867 +873 @@ class FrameProcessor:
-        generator = HTMLFrameGenerator(template_path)
+        generator = HTMLFrameGenerator(str(template_path))
diff --git a/pixelle_video/services/video.py b/pixelle_video/services/video.py
index e59d5b4..2376a8f 100644
--- a/pixelle_video/services/video.py
+++ b/pixelle_video/services/video.py
@@ -113 +113 @@ class VideoService:
-        method: Literal["demuxer", "filter"] = "demuxer",
+        method: Literal["demuxer", "filter"] = "filter",
@@ -299,0 +300,9 @@ class VideoService:
+                '-c:v', 'libx264',
+                '-preset', 'fast',
+                '-crf', '20',
+                '-pix_fmt', 'yuv420p',
+                '-c:a', 'aac',
+                '-b:a', '192k',
+                '-ar', '44100',
+                '-ac', '2',
+                '-movflags', '+faststart',
