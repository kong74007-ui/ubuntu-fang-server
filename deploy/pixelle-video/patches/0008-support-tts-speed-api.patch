diff --git a/api/routers/video.py b/api/routers/video.py
index e01bcae..01eed28 100644
--- a/api/routers/video.py
+++ b/api/routers/video.py
@@ -158,6 +158,7 @@ async def generate_video_sync(
             "prompt_prefix": request_body.prompt_prefix,
             "bgm_path": request_body.bgm_path,
             "bgm_volume": request_body.bgm_volume,
+            "tts_speed": request_body.tts_speed,
         }
         asset_ids, external_segments = _lease_external_segments(
             request_body, f"sync-{uuid.uuid4().hex}"
@@ -298,6 +299,7 @@ async def generate_video_async(
                 "prompt_prefix": request_body.prompt_prefix,
                 "bgm_path": request_body.bgm_path,
                 "bgm_volume": request_body.bgm_volume,
+                "tts_speed": request_body.tts_speed,
                 # Progress callback can be added here if needed
                 # "progress_callback": lambda event: task_manager.update_progress(...)
             }
diff --git a/api/schemas/video.py b/api/schemas/video.py
index 6ecf5e9..4d202d0 100644
--- a/api/schemas/video.py
+++ b/api/schemas/video.py
@@ -54,6 +54,7 @@ class VideoGenerateRequest(BaseModel):
         None,
         description="(Deprecated) TTS voice ID for legacy compatibility"
     )
+    tts_speed: float = Field(default=1.0, ge=0.5, le=2.0, description="TTS playback speed multiplier")
     tts_voice: Optional[str] = Field(None, description="TTS voice name")
     narration_segments: Optional[list[NarrationSegment]] = Field(
         None,
